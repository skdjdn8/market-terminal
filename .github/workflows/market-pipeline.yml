name: Market Data + Research Pipeline

on:
  schedule:
    # Data fetch every 30 minutes
    - cron: '*/30 * * * *'
    # Daily report at 00:30 UTC (08:30 Beijing)
    - cron: '30 0 * * *'
  workflow_dispatch:  # manual trigger from GitHub UI

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    if: github.event.schedule != '30 0 * * *' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Fetch market data
        env:
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
        run: node fetch-and-report.js
      - name: Commit data
        run: |
          git config user.name "market-bot"
          git config user.email "bot@market.terminal"
          git add data.json
          git diff --staged --quiet || git commit -m "data: auto-update $(date -u +%Y-%m-%dT%H:%MZ)"
          git push

  daily-report:
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 0 * * *' || (github.event_name == 'workflow_dispatch' && github.event.inputs.report == 'true')
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - name: Run research pipeline
        env:
          FINNHUB_KEY: ${{ secrets.FINNHUB_KEY }}
          DEEPSEEK_KEY: ${{ secrets.DEEPSEEK_KEY }}
        run: node fetch-and-report.js --report
      - name: Commit report
        run: |
          git config user.name "market-bot"
          git config user.email "bot@market.terminal"
          git add data.json report.md report.json
          git diff --staged --quiet || git commit -m "report: daily research $(date -u +%Y-%m-%d)"
          git push
